@import '../variables.styl'

.CatList-container
  background-color lighten(color-lightest, 60)
  position relative
  display flex
  flex-flow row wrap
  padding 5px
  list-style-type none
  
  .CatList-controls
    
    select, input, span
      margin 0 20px
  
  .CatList-empty
    margin 20px
    text-align center
    font-style italic
  
